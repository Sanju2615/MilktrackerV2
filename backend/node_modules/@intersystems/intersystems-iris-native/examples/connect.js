const { native } = require('./config/_import.js')
const { logDir } = require('./config/log_dir.js')
const { conOptions } = require('./config/connection_options.js')

const path = require('path')
const logfilename = __filename.split(path.sep).pop().replace('.js', '.log')
conOptions.logfile = path.join(logDir, logfilename)

const conn = native.createConnection(conOptions)
console.log('Connected. Now closing.')
console.log(`${conn}`)
console.log('shared memory: ', conn.isUsingSharedMemory())
conn.close()

// alternatively using callback:

// native.createConnection(conOptions, (err, c) => {
//   if (err) {
//     console.log('Could not establish connection: ' + err)
//   } else {
//     // we have a valid connection - dispatch to do work? Or do it here
//     if (!c.isClosed()) {
//       console.log('Connected to host')
//       const iris = c.createIris()
//       iris.set('hello from an asynchronously connected world', 'isc.test', 1, 2)
//       console.log(iris.get('isc.test', 1, 2))
//     } else {
//       console.log('Connection object instantiated but node open!')
//     }
//   }
// })
// // this happens after sending the async request
// console.log('Connection request sent to IRIS')
