// export { // version > 14.3 required 
//   Iris, 
//   Iterator, 
//   IRISGlobalNode,
//   GatewayUtility, 
//   IRISList, 
//   IRISReference, 
//   Connection, 
//   IRISObject,
//   createConnection,
//   connect 
// } from './index.js'

// export { default } from './index.js' // this allows for "import irissdk from intersystems-iris" 
// where default is automatically replaced with irissdk
// however if you call import() this will return the object { default: [] } which means we have to 
// do a .default to get the content. So unless I figure out the import I will leave it as follows:

// export { default as irissdk } from './index.js'
// this forces us to use {} when we import as follows: import { irissdk } from 'intersystems-iris'

// finally an actual solution 

import irissdk from './index.js'
export const { // tested with 12.19.0 and higher
  Iris, 
  Iterator, 
  IRISGlobalNode,
  IRISList, 
  IRISReference, 
  Connection, 
  IRISObject,
  createConnection,
  connect 
} = irissdk

console.log('loaded as ESM')